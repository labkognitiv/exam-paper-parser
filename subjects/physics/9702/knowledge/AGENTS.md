# Physics Knowledge

Follow `../AGENTS.md`, then read `session.md`.

This layer owns the syllabus-derived curriculum spine, controlled knowledge,
contracts and templates. Preserve stable IDs and accepted definitions. Lessons
belong in `../study/`; official paper packages belong in `../past papers/`.

Use `skills/` only for knowledge-owned workflows. Record material changes in
`session.md`. Unmatched historical sources remain in repository-root
`archive/2026-09-05-physics-math-alignment/`.
