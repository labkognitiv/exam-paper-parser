# Physics 9702

Follow repository instructions. Read the nearest layer `AGENTS.md` and recent
session log before material work.

- Active areas: `knowledge/`, `study/`, `past papers/`, `reviews/`,
  `prototypes/`, and `planner/`.
- `knowledge/` owns the syllabus spine and controlled Physics registries.
- `study/` owns topic, module and lesson packages.
- `past papers/` owns canonical P1, P2 and P4 extraction plus enrichment.
- `reviews/` owns review state, evidence and benchmarks; it is not source truth.
- Superseded structure is retained under repository-root
  `archive/2026-09-05-physics-math-alignment/` as historical evidence, not an
  active fallback path.
- Preserve whole questions, stable IDs, dependencies and official mark schemes.
- P1 uses one complete MCQ PNG, TXT and JSON plus the official answer.
- Physics formal definitions must exactly match
  `knowledge/knowledge-base/definitions.json`.
- Keep `numerical_values_checked` unchanged without authorized numerical review.
- Physics workflows belong in the active owning layer's `skills/` folder.
- Append one concise dated handoff to the owning layer log after material work.
