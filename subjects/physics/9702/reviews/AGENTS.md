# Physics Reviews

Follow `../AGENTS.md`, then read `SESSION-LOG.md`.

Reviews contain findings and decisions, evidence contains workflow proof, and
benchmarks evaluate workflows. None replaces canonical content or makes a
historical PASS a fresh validation.

Verification workflows belong in `skills/`. Preserve unresolved states and
record material changes in `SESSION-LOG.md`.
