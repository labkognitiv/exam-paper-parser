# Physics Study

Follow `../AGENTS.md`, then read `SESSION-LOG.md`.

Study owns the ordered topic, course-module, module and lesson hierarchy plus
shared lesson assets. Preserve stable IDs, page order, question dependencies and
distinct notes, questions, mark schemes and enrichment. Do not duplicate
canonical papers or controlled knowledge here.

The active syllabus-wide lesson plan is the 147-lesson, 25-topic map after the
2026-09-10 controlled consolidation. Retired IDs resolve through
`lesson-id-redirects.json`; preserve active IDs, compatibility records and paths.

Use `orchestration.json` with `skills/lesson-workflow.md`: Plan → Author → Review
Student → Review Science → manager Topic Audit → Visualize/Create Questions →
Review Questions. One persistent local task per lesson; never worktrees. Reviewers
are independent; only the manager writes shared tracker/session files in a run.
Use `skills/subject-system.md` for Physics source, vocabulary and validator rules.
Preserve lesson/section order, active IDs, redirects and historical approvals.
Student-reference sync is outside production; retain existing registry/history.
Record material changes in `SESSION-LOG.md`.
